-- ============================================================
-- key_results.started_date
-- ============================================================
-- 이 KR (선행 활동) 의 시작일.
-- 비어있으면 사이클 시작일로 fallback.
-- 12주 주별 타겟의 실제 날짜는 started_date 기준으로 계산.
-- ============================================================

ALTER TABLE key_results
  ADD COLUMN IF NOT EXISTS started_date DATE;
